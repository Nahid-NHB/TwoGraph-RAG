/**
 * @twograph/core — shared domain model for TwoGraph-RAG.
 *
 * Domain types, the stable symbol ID codec, configuration loading, and the
 * typed error hierarchy land here in subsequent issues (#7, #8).
 */

export const PACKAGE_NAME = '@twograph/core';
