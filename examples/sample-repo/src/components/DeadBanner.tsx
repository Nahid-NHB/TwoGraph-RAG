/** DEAD CODE (intentional): promo banner that is never rendered anywhere. */
export function DeadBanner() {
  return (
    <aside>
      <p>This banner is unreachable and should be flagged by dead-code detection.</p>
    </aside>
  );
}
