## Summary

<!-- What does this PR do, and why? -->

Closes #<!-- issue number — required, per CONTRIBUTING.md's one-issue-per-PR workflow -->

## Checklist

- [ ] This PR closes exactly one issue (linked above with `Closes #N`)
- [ ] Its dependencies (see the issue's "Dependencies" section) are already merged
- [ ] Tests added/updated; `pnpm test` (and `pnpm test:int` if applicable) pass locally
- [ ] `pnpm lint && pnpm lint:boundaries && pnpm typecheck` pass locally
- [ ] Docs updated (`docs/`, README) if this PR changes behavior they describe
- [ ] A changeset was added (`pnpm changeset`) if this is a user-facing change

## Test plan

<!-- How did you verify this? Commands run, scenarios exercised. -->
